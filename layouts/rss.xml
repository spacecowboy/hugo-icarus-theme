<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:webfeeds="http://webfeeds.org/rss/1.0"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ with .Title }}in {{.}} {{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Params.author_email }}
    <managingEditor>{{.}}{{ with $.Site.Params.author }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Params.author_email }}
    <webMaster>{{.}}{{ with $.Site.Params.author }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    <atom:link href="{{.Site.RSSLink | absURL}}" rel="self" type="application/rss+xml" />
    {{ range .AlternativeOutputFormats -}}
    <atom:link rel="alternate" type="{{ .MediaType.Type }}" href="{{ .Permalink | safeURL }}" />
    {{ end -}}
    {{ with $.Site.Params.cover_image }}<webfeeds:cover image="{{ . | absURL }}" />{{end}}
    {{ with $.Site.Params.logo }}<webfeeds:icon>{{ . | absURL }}</webfeeds:icon>{{end}}
    {{ range first 15 .Data.Pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with $.Site.Params.author_email }}<author>{{.}}{{ with $.Site.Params.author }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      {{ with .Params.banner }}
      <media:thumbnail url="{{. | absURL }}" />
      {{end}}
      <description>
        {{ .Content | html }}
        {{ partial "series.html" . | html }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
